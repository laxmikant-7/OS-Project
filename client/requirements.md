## Packages
framer-motion | Smooth layout animations for process table and dashboard cards
recharts | Visualization for CPU usage and wait time distributions
date-fns | Formatting timestamps for logs and history

## Notes
WebSocket connection required for real-time simulation updates (state_update, metrics_update).
Theme is dark mode by default with glassmorphism effects.
